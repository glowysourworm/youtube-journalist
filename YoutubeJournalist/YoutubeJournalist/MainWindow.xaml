<Window x:Class="YoutubeJournalist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YoutubeJournalist"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="250" />
        </Grid.RowDefinitions>

        <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Google Api Key" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" />
            <TextBox IsReadOnly="True" Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding ApiKey}" Margin="5" HorizontalAlignment="Stretch" Height="32" FontSize="14" />

            <TextBlock Grid.Row="1" Text="Search" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" />
            <TextBox x:Name="SearchTB" Grid.Row="1" Grid.Column="1" VerticalContentAlignment="Center" Margin="5" HorizontalAlignment="Stretch" Height="32" FontSize="14" />

            <Button Grid.Row="3" Grid.Column="1" Click="Button_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="5" Padding="10">Search</Button>
            
        </Grid>
        
        

        <ListBox x:Name="OutputLB" Grid.Row="1" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Padding="3" FontSize="14" ToolTip="{Binding ContentOwnerDetails_ContentOwner}">
                        <Run Text="{Binding Id}" />
                        <Run Text="{Binding Kind}" />
                        <Run Text="{Binding ETag}" />
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
    </Grid>
</Window>
