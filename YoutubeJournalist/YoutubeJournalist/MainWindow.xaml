<Window x:Class="YoutubeJournalist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YoutubeJournalist"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="250" />
        </Grid.RowDefinitions>

        <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Google Api Key" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" />
            <TextBox IsReadOnly="True" Grid.Column="1" VerticalContentAlignment="Center" Text="{Binding ApiKey}" Margin="5" HorizontalAlignment="Stretch" Height="32" FontSize="14" />

            <TextBlock Grid.Row="1" Text="Search / Filter" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" />
            <TextBox x:Name="SearchTB" Grid.Row="1" Grid.Column="1" VerticalContentAlignment="Center" Margin="5" HorizontalAlignment="Stretch" Height="32" FontSize="14" />

            <StackPanel Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
                <Button x:Name="SearchChannelsButton" Click="SearchChannelsButton_Click" Margin="5" Padding="10">Search Channels</Button>
                <Button x:Name="SearchVideosButton" Click="SearchVideosButton_Click" Margin="5" Padding="10">Search Videos</Button>
                <Button x:Name="SearchPlaylistsButton" Click="SearchPlaylistsButton_Click" Margin="5" Padding="10">Search Playlists</Button>
                <Button x:Name="FilterChannelsButton" Click="FilterChannelsButton_Click" Margin="5" Padding="10">Filter Channels</Button>
                <Button x:Name="FilterVideosButton" Click="FilterVideosButton_Click" Margin="5" Padding="10">Filter Videos</Button>
            </StackPanel>
            
            
        </Grid>
        
        

        <ListBox x:Name="OutputLB" Grid.Row="1" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Padding="3" FontSize="14" ToolTip="{Binding Description}">
                        <Image Source="{Binding Thumbnail}" Height="20" Width="30" />
                        <Run Text="{Binding Id}" />
                        <Run Text="{Binding Title}" />
                        <Run Text="{Binding Created}" />
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
    </Grid>
</Window>
